<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <!-- Permissions untuk alarm -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- Permission untuk boot completed -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" /> <!-- Untuk Android 12+ -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TugasPTS"
        android:usesCleartextTraffic="true"
        tools:targetApi="31" >
        <activity
            android:name=".AlarmRestorationJobService"
            android:exported="false" />
        <!-- Main Activity (Launcher) -->
        <activity
            android:name=".MainActivity2"
            android:exported="true"
            android:theme="@style/Theme.TugasPTS" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Login Activity -->
        <activity
            android:name=".Login"
            android:exported="false"
            android:theme="@style/Theme.TugasPTS" /> <!-- World Clock Activity -->
        <activity
            android:name=".MainActivity3"
            android:exported="false"
            android:theme="@style/Theme.TugasPTS" /> <!-- Timer Activity -->
        <activity
            android:name=".MainActivity4"
            android:exported="false"
            android:theme="@style/Theme.TugasPTS" /> <!-- Stopwatch Activity -->
        <activity
            android:name=".MainActivity5"
            android:exported="false"
            android:theme="@style/Theme.TugasPTS" /> <!-- Settings/Profile Activity -->
        <activity
            android:name=".MainActivity6"
            android:exported="false"
            android:theme="@style/Theme.TugasPTS" /> <!-- Add/Edit Alarm Activity -->
        <activity
            android:name=".MainActivity7"
            android:exported="false"
            android:theme="@style/Theme.TugasPTS" /> <!-- Profile Activity -->
        <activity
            android:name=".Logout"
            android:exported="false"
            android:theme="@style/Theme.TugasPTS" /> <!-- Timer Running Activity -->
        <activity
            android:name=".TimerRunningActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.TugasPTS" /> <!-- Receivers untuk alarm -->
        <!-- Alarm Receiver - HANYA untuk trigger alarm -->
        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" >

            <!-- TIDAK ADA intent filter di sini -->
        </receiver> <!-- Alarm Dismiss Receiver - Untuk tombol "Tutup" di notifikasi -->
        <receiver
            android:name=".AlarmDismissReceiver"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" /> <!-- Alarm Snooze Receiver - Untuk tombol "Tunda" di notifikasi -->
        <receiver
            android:name=".AlarmSnoozeReceiver"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" /> <!-- Boot Receiver - HANYA untuk restart alarm setelah device reboot -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>
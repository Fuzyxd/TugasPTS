<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#EA000000">

    <!-- MAIN CARD - DI TENGAH LAYAR -->
    <View
        android:id="@+id/main_card"
        android:layout_width="325dp"
        android:layout_height="400dp"
        android:background="@drawable/login_card_bg"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="0dp"
        android:layout_marginBottom="0dp" />

    <!-- PROFILE TITLE -->
    <TextView
        android:id="@+id/tv_profile"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Profile"
        android:textColor="#FFFFFF"
        android:textSize="48sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="@id/main_card"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="18dp" />

    <!-- PROFILE IMAGE -->
    <ImageView
        android:id="@+id/iv_profile_large"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/profile_icon"
        android:background="@drawable/circle_background"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toBottomOf="@id/tv_profile"
        app:layout_constraintStart_toStartOf="@id/main_card"
        app:layout_constraintEnd_toEndOf="@id/main_card"
        android:layout_marginTop="30dp" />

    <!-- USERNAME TEXT -->
    <TextView
        android:id="@+id/tv_username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Users"
        android:textColor="#FFFFFF"
        android:textSize="32sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/iv_profile_large"
        app:layout_constraintStart_toStartOf="@id/main_card"
        app:layout_constraintEnd_toEndOf="@id/main_card"
        android:layout_marginTop="20dp" />

    <!-- LOGOUT BUTTON -->
    <View
        android:id="@+id/btn_logout_bg"
        android:layout_width="286dp"
        android:layout_height="47dp"
        android:background="@drawable/login_button_bgred"
        app:layout_constraintTop_toBottomOf="@id/tv_username"
        app:layout_constraintStart_toStartOf="@id/main_card"
        app:layout_constraintEnd_toEndOf="@id/main_card"
        android:layout_marginTop="40dp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintTop_toTopOf="@id/btn_logout_bg"
        app:layout_constraintBottom_toBottomOf="@id/btn_logout_bg"
        app:layout_constraintStart_toStartOf="@id/btn_logout_bg"
        app:layout_constraintEnd_toEndOf="@id/btn_logout_bg"
        android:gravity="center">

        <ImageView
            android:id="@+id/iv_logout_icon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/logout"
            android:layout_marginEnd="8dp"
            android:tint="#000000" />

        <TextView
            android:id="@+id/tv_logout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="LOGOUT"
            android:textColor="#000000"
            android:textSize="14sp"
            android:textStyle="bold" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>